[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "packrelic-native"
version = "0.1.0"
description = "Native byte scanning helpers for PackRelic."
requires-python = ">=3.10"

[tool.maturin]
module-name = "packrelic_native"
bindings = "pyo3"
