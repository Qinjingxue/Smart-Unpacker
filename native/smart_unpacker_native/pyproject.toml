[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "smart-unpacker-native"
version = "0.1.0"
description = "Native byte scanning helpers for Smart Unpacker."
requires-python = ">=3.10"

[tool.maturin]
module-name = "smart_unpacker_native"
bindings = "pyo3"
