[workspace]
members = [
    "sunpack_native",
    "sunpack_usn_core",
    "sunpack_watch_broker",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
